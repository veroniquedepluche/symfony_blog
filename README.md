# Projet de cours - Symfony Blog

Aplication **Symfony 4.4**.

Pour installer:
```sh
git clone URL
```
>Récupérer `URL` en cliquant sur le bouton *Code*.

Ensuite, installer les dépendances **PHP** avec **Composer**:
```sh
composer install
```